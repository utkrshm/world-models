[project]
name = "world-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Utkarsh (Ubuntu)", email = "malaiya.utkarsh@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
world-models = "world_models:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
